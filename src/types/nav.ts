export type NavItemListProps = {
  href: string,
  imgLink: string,
  innerText: string
}

export type NewsProps = {
  id: string,
  title: string,
  image: string,
  category: string,
  author: string,
  authorPic: string,
  published_date: string,
  content: string,
  tags: string[]
}